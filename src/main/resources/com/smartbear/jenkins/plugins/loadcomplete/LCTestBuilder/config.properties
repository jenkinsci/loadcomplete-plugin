Suite = Project suite file:

Project = Project:
Test = Test name:

ExecutorSettings = Settings
GeneratePDF = Generate PDF log file
GenerateMHT = Generate MHT log file

UseTestTimeout = Use test timeout
TimeoutInSeconds = Timeout (seconds):
Executor = Test runner:
ExecutorVersion = LoadComplete version:
ActionOnWarningsTitle = Action on warnings:
ActionOnErrorsTitle = Action on errors:

ProjectDescription = Specify the path relative to the job''s workspace folder on the node (for example, MyProjects\\Project.ltp).
TestDescription = Example: "Test1".

LinkToHelp = <p style="margin: 8px 0 16px 0;"> For information on creating LoadComplete test steps, see <a href="http://wiki.jenkins-ci.org/display/JENKINS/LoadComplete+Support+Plugin">LoadComplete documentation</a>.</p>
